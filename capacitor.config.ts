import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Cripto-Doc-RD',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
